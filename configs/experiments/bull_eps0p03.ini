# Bull path with level_K = 0.03 K: the calibration-ladder threshold for the
# 1e-6 error target (Table I, Lseg 1.2 mm row: eps 0.032 K -> rel-L2 5.9e-7).
# Everything else matches fast_heat.ini, isolating epsilon's effect on
# strong-scaling speedup.
[include]
base = ../examples/fast_heat.ini

[dependency]
level_K = 0.03
