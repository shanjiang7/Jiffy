# Bull path with the paper-era retention threshold (level_K = 0.1 K) on the
# current DAG construction. Used to isolate the effect of epsilon on strong
# scaling: everything else (Lseg, ROI, chords pair test, chord lookup source)
# matches configs/examples/fast_heat.ini.
[include]
base = ../examples/fast_heat.ini

[dependency]
level_K = 0.1
