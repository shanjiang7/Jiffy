# Visualization-only variant for the scan-path overview figure: scan-line
# spacing coarsened 2.5x (500um) so the serpentine structure is visible at
# figure scale. Not used for any measurement.
[include]
base = ../examples/fast_heat.ini

[path.picture]
image = ../images/longhorn.jpg
column_res_x = 500um
column_res_y = 500um
