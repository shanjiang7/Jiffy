# Bull path with retention threshold level_K = 1 K. Mid-loose point of the
# epsilon sweep (between 5 K and 0.1 K). Everything else (Lseg, ROI, chords pair
# test, chord lookup source) matches fast_heat.ini, isolating epsilon's effect
# on strong-scaling speedup.
[include]
base = ../examples/fast_heat.ini

[dependency]
level_K = 1.0
