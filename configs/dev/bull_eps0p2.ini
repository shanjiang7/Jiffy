# Bull path with level_K = 0.2 K: the calibration-ladder threshold for the
# 1e-5 error target (Table I, Lseg 1.1 mm row: eps 0.193 K -> rel-L2 3.4e-6).
# Everything else matches fast_heat.ini, isolating epsilon's effect on
# strong-scaling speedup.
[include]
base = ../examples/fast_heat.ini

[dependency]
level_K = 0.2
