# One unit of the continuous spiral-raster path (repeats = 1), used for the
# scan-path overview figure (dev/plot_scan_paths.py). Everything else
# matches examples/continuous_hybrid.ini.
[include]
base = ../examples/continuous_hybrid.ini

[path.continuous_hybrid]
repeats = 1
