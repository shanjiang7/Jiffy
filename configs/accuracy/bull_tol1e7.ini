# Bull path accuracy study (paper Table V): target rel-L2 = 1e-7.
# Calibrated pair from Table I: Lseg = 1.3 mm (130 steps), epsilon = 0.01 K —
# identical to the path_base defaults; this file exists so job scripts can
# name the tolerance explicitly.
[include]
base = ../examples/bull.ini
