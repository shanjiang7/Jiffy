# A/B variant: identical to hybrid_spiral_raster_tol1e4.ini but with the
# published AABB pair test instead of the chord-based test.
[include]
base = hybrid_spiral_raster_tol1e4.ini

[dependency]
pair_test = aabb
# Published lookup: single-pulse mock source (pre-chords behaviour).
lookup_source_on_steps = 1
