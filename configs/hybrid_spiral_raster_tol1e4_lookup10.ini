# Chords DAG with a 10-step mock source for the r_eps lookup
# (point-like source: 0.1 mm of track, 5 cells at the 20 um lookup grid).
[include]
base = hybrid_spiral_raster_tol1e4.ini

[dependency]
lookup_source_on_steps = 10
