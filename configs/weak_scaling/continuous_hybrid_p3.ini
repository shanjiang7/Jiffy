# Weak scaling, continuous-hybrid (spiral-raster) path: the problem grows
# with the rank count by repeating the unit, so a 3-rank run scans 3
# unit(s) and the work per rank stays constant.
[include]
base = ../examples/continuous_hybrid.ini

[path.continuous_hybrid]
repeats = 3
