# Weak scaling, Bull path (paper Sec. V-D): the horizontal extent is scaled by
# sqrt(P) so the scanned area - and hence the work - grows linearly with the
# rank count. P = 5: 50 mm * sqrt(5) = 111.803 mm.
[include]
base = ../examples/fast_heat.ini

[path.picture]
horizontal = 111.803mm
