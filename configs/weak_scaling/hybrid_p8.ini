# Weak scaling, hybrid spiral-raster (paper Sec. V-D): the problem grows with
# the rank count by repeating the motif, so a 8-rank run scans 8 motif(s)
# and the work per rank stays constant.
[include]
base = ../examples/hybrid_spiral_raster.ini

[path.hybrid_spiral_raster]
repeats = 8
