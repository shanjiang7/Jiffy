# Archived development scripts

Historical experiment drivers kept for provenance. Most reference the
discontinuous hybrid spiral-raster path (removed from the live tree in the
camera-ready path refinement) or pre-camera-ready protocols, and will not
run against the current configs. The camera-ready measurement pipeline is
the `dev/run_cr_*` family plus the `dev/plot_*` scripts one level up.
